---
import BtnLink from "../shared/BtnLink.astro";
import Button from "../shared/Button.astro";
import Container from "../shared/Container.astro";
import CtaButton from "../shared/CtaButton.astro";
import GradiantText from "../shared/GradiantText.astro";
import Paragraph from "../shared/Paragraph.astro";

export interface Props {
  className?: string;
}

const { className } = Astro.props;
---

<section id="ctaOrder" class=`pb-20 ${className}`>
  <Container>
    <div
      class="w-full relative py-8 md:py-10 px-6 md:px-8 rounded-2xl bg-gradient-to-br from-slate-100 to-zinc-200 dark:from-slate-800 dark:to-zinc-800"
    >
      <div class="mx-auto text-center max-w-xl md:max-w-2xl relative">
        <h1
          class="text-3xl/tight sm:text-4xl/tight md:text-5xl/tight
          font-bold text-heading-1"
        >
          Gotowy na Zamówienie

          <!-- <GradiantText> Zamówienie </GradiantText> -->
          <GradiantText> Domu </GradiantText>
          dla
          <GradiantText> Twojego Pupila </GradiantText>
          ?
        </h1>
        <Paragraph className="pt-10 font-semibold">
          Zadzwoń już dziś i zamów. Zamówinia realizujemy w ciągu 7 dni roboczych, których potrzebujemy na stworzenie twojej budy. Następnie wysyłamy ją do Twojego domu, lub sam wygodnie odbierasz ją z naszej stolarni.
        </Paragraph>
        <div class="mx-auto max-w-md sm:max-w-xl pt-10">
          <CtaButton />

        </div>
          <div class="mt-4">
            <BtnLink
              text="Zamów przez formularz" 
              href="/order"
              className="flex justify-center w-full sm:w-max m-auto"
              variant="primary"
            />
          </div>


      </div>
    </div>
  </Container>
</section>
